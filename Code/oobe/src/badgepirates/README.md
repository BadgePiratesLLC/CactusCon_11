# BadgePirates Common Code

Changes required in common code files:

* `ota.cpp` - Set up code to indicate different steps/errors during updates
* `factory.cpp` - Define any user code for peripherals.